<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css" />
</head>
<body>
  <div class="container d-flex align-items-center justify-content-center vh-100">
    <div class="text-center border rounded p-4">
      <h1 class="mb-4">Signup</h1>
      <form action="/user/signup" method="POST">
        <div class="mb-3">
          <label for="username" class="form-label">Username:</label>
          <input type="text" id="username" name="username" class="form-control" required>
                    <small class="form-text text-muted">Username should not contain any numbers or characters</small>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Password:</label>
          <input type="password" id="password" name="password" class="form-control" required>
           <small class="form-text text-muted">Password should be 6 to 15 characters long</small>
        </div>

        <div class="form-check mb-3">
          <input type="checkbox" id="author" name="author" class="form-check-input">
          <label for="author" class="form-check-label">Author</label>
        </div>

        <% if (errors && errors.length > 0) { %>
          <% errors.forEach(error => { %>
            <p><%= error.msg %></p>
          <% }) %>
        <% } %>

        <input type="submit" value="Signup" class="btn btn-primary">
        <a href="/user/login" class="btn btn-secondary">Already a user? Login</a>
      </form>
    </div>
  </div>

  <!-- Add Bootstrap JavaScript and its dependencies -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>







  
</body>
</html>